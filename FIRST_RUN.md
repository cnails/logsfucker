# 🚀 Первый запуск LogsFucker

Следуйте этим простым шагам для первого запуска проекта.

## ⚡ Быстрый путь (для опытных)

```bash
# 1. Backend
cd backend
npm install
wrangler d1 create logs-db
# Обновите database_id в wrangler.toml
npm run d1:migrate
npm run dev &

# 2. Frontend
cd ../frontend
npm install
npm run dev
```

Откройте: http://localhost:3000

## 📋 Пошаговая инструкция (для новичков)

### Шаг 1: Проверка требований

Убедитесь, что установлено:

```bash
node --version  # Должно быть >= 18.0.0
npm --version   # Должно быть >= 9.0.0
```

Если нет, установите Node.js с https://nodejs.org/

### Шаг 2: Настройка Backend

#### 2.1. Установка зависимостей

```bash
cd backend
npm install
```

Это займет ~1-2 минуты.

#### 2.2. Вход в Cloudflare

```bash
npx wrangler login
```

Откроется браузер для авторизации. Войдите в ваш Cloudflare аккаунт (или создайте бесплатный).

#### 2.3. Создание базы данных

```bash
npx wrangler d1 create logs-db
```

**Важно!** Скопируйте вывод команды. Он будет выглядеть так:

```
✅ Successfully created DB 'logs-db'

[[d1_databases]]
binding = "DB"
database_name = "logs-db"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
```

#### 2.4. Обновление конфигурации

Откройте файл `backend/wrangler.toml` в редакторе и найдите секцию:

```toml
[[d1_databases]]
binding = "DB"
database_name = "logs-db"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"  # ⬅️ Замените этот ID
```

Замените `database_id` на ID из предыдущего шага.

#### 2.5. Применение миграций

```bash
npm run d1:migrate
```

Вы должны увидеть:

```
✅ Successfully applied 1 migration(s)
```

#### 2.6. Генерация тестовых данных (опционально)

```bash
node scripts/generate-test-data.js
```

Это создаст ~150 тестовых логов для демонстрации интерфейса.

#### 2.7. Запуск Backend

```bash
npm run dev
```

Backend запустится на http://localhost:8787

**Не закрывайте этот терминал!** Оставьте его работать.

### Шаг 3: Настройка Frontend

Откройте **новый терминал** (оставьте backend работать).

#### 3.1. Установка зависимостей

```bash
cd frontend  # или cd ../frontend если вы в backend/
npm install
```

Это займет ~2-3 минуты.

#### 3.2. Запуск Frontend

```bash
npm run dev
```

Frontend запустится на http://localhost:3000

### Шаг 4: Открытие в браузере

Откройте http://localhost:3000 в вашем браузере.

Вы увидите:
- 🔥 Дерзкий интерфейс в темных и фиолетовых тонах
- 📊 Панель со статистикой
- 🚨 Таблицу аномальных IP
- 📋 Таблицу логов

## ✅ Проверка работы

### Проверка Backend

В новом терминале:

```bash
curl http://localhost:8787/api/logs?limit=5
```

Должен вернуть JSON с логами (или пустой массив `[]`).

### Проверка Frontend

1. Откройте http://localhost:3000
2. Вы должны увидеть дашборд
3. Попробуйте изменить фильтры
4. Нажмите кнопку "🚀 Обновить данные"

## 🐛 Проблемы?

### "wrangler: command not found"

```bash
cd backend
npm install
npm run dev  # используйте npm run, а не wrangler напрямую
```

### "database_id is required"

Убедитесь, что вы:
1. Создали базу: `wrangler d1 create logs-db`
2. Скопировали `database_id` в `wrangler.toml`
3. Сохранили файл

### Frontend не показывает данные

1. Убедитесь, что backend запущен (http://localhost:8787)
2. Сгенерируйте тестовые данные:
   ```bash
   cd backend
   node scripts/generate-test-data.js
   ```
3. Обновите страницу в браузере

### "Module not found" в frontend

```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
```

### "CORS error"

Убедитесь, что:
- Backend на порту 8787
- Frontend на порту 3000
- Оба сервиса запущены

## 🎉 Готово!

Теперь у вас работает:
- ✅ Backend API на http://localhost:8787
- ✅ Frontend дашборд на http://localhost:3000
- ✅ База данных D1 с тестовыми данными
- ✅ Полный стек LogsFucker!

## 🚀 Что дальше?

### Для изучения

1. **Поэкспериментируйте с фильтрами** - попробуйте разные временные периоды
2. **Посмотрите код** - изучите компоненты в `frontend/src/components/`
3. **Прочитайте документацию** - см. [README.md](./README.md)

### Для разработки

1. **Прочитайте DEV.md** - руководство разработчика
2. **Изучите API** - см. [backend/README.md](./backend/README.md)
3. **Добавьте свои фичи** - см. [CONTRIBUTING.md](./CONTRIBUTING.md)

### Для продакшена

1. **Деплой backend** - см. [backend/DEPLOYMENT.md](./backend/DEPLOYMENT.md)
2. **Деплой frontend** - см. [frontend/README.md](./frontend/README.md#деплой-на-cloudflare-pages)
3. **Настройте мониторинг** - добавьте алерты и метрики

## 💡 Полезные команды

```bash
# Остановка серверов
# Нажмите Ctrl+C в терминалах с backend и frontend

# Перезапуск
npm run dev  # из корня проекта (запустит оба)

# Генерация новых тестовых данных
cd backend
node scripts/generate-test-data.js

# Проверка логов backend
curl http://localhost:8787/api/logs?limit=10

# Проверка статистики
curl http://localhost:8787/api/stats
```

## 📚 Документация

- [QUICKSTART.md](./QUICKSTART.md) - быстрый старт
- [README.md](./README.md) - основная документация
- [DEV.md](./DEV.md) - руководство разработчика
- [INSTALLATION.md](./INSTALLATION.md) - детальная установка
- [frontend/README.md](./frontend/README.md) - документация фронтенда

---

**Поздравляем!** Вы успешно запустили LogsFucker! 🔥

Made with 💜 and ⚡ by LogsFucker Team

