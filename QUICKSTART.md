# 🚀 Быстрый старт

Этот гайд поможет запустить полный стек LogsFucker (Backend + Frontend) за 10 минут.

## 🔧 Backend

### Шаг 1: Перейдите в папку backend

```bash
cd backend
```

## Шаг 2: Установка зависимостей

```bash
npm install
```

## Шаг 3: Создание D1 базы данных

```bash
# Создайте D1 базу
wrangler d1 create logs-db

# Скопируйте database_id из вывода команды
# и вставьте его в wrangler.toml
```

Обновите `backend/wrangler.toml`:

```toml
[[d1_databases]]
binding = "DB"
database_name = "logs-db"
database_id = "ваш-реальный-database-id"  # ⬅️ Вставьте сюда
```

## Шаг 4: Применение миграций

```bash
npm run d1:migrate
```

Вы должны увидеть:

```
✅ Successfully applied 1 migration(s)
```

## Шаг 5: Локальная разработка

```bash
npm run dev
```

Backend готов! Теперь переходим к фронтенду.

---

## 🎨 Frontend

### Шаг 1: Перейдите в папку frontend

```bash
cd ../frontend  # если вы в backend/
# или
cd frontend     # если вы в корне проекта
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

### Шаг 3: Запуск в режиме разработки

```bash
npm run dev
```

Frontend будет доступен на: **http://localhost:3000**

### Шаг 4: Откройте браузер

Откройте http://localhost:3000 и наслаждайтесь дерзким интерфейсом! 🔥

**Важно**: Убедитесь, что backend запущен на порту 8787 (из предыдущего шага).

---

## 🚀 Деплой

### Backend

```bash
cd backend

```bash
npm run deploy
```

После деплоя обновите URL в `example-client.html` на ваш реальный адрес Pages.

### Frontend

```bash
cd frontend
npm run build
```

Затем подключите репозиторий к Cloudflare Pages:
- Build command: `cd frontend && npm install && npm run build`
- Build output directory: `frontend/dist`

Подробнее: [frontend/README.md](./frontend/README.md#деплой-на-cloudflare-pages)

## Проверка работы

### Отправить тестовый лог

```bash
curl -X POST http://localhost:8788/api/logs \
  -H "Content-Type: application/json" \
  -d '{
    "extensionName": "test",
    "level": "info",
    "message": "Hello World"
  }'
```

### Получить логи

```bash
curl http://localhost:8788/api/logs?limit=10
```

### Получить статистику

```bash
curl http://localhost:8788/api/stats
```

## Структура файлов

```
logsfucker/
├── backend/
│   ├── functions/api/
│   │   ├── logs.ts          # POST, GET логов
│   │   └── stats.ts         # GET статистики
│   ├── migrations/
│   │   └── 0001_init.sql    # SQL миграция
│   ├── scripts/
│   │   └── generate-test-data.js  # Генератор тестовых данных
│   ├── wrangler.toml        # Конфиг Cloudflare
│   ├── package.json
│   ├── tsconfig.json
│   └── example-client.html  # Тестовый UI
└── frontend/                # React + TypeScript
    ├── src/
    │   ├── components/      # Filters, Stats, Anomalies, Logs
    │   ├── hooks/           # useStats, useLogs
    │   ├── pages/           # Dashboard
    │   └── types/           # TypeScript типы
    └── package.json
```

## Частые проблемы

### ❌ "database_id is required"

Убедитесь, что в `backend/wrangler.toml` указан реальный `database_id` из команды `wrangler d1 create`.

### ❌ "no such table: logs"

Запустите миграции: `npm run d1:migrate`

### ❌ "CORS error" при локальной разработке

Убедитесь, что сервер запущен через `npm run dev`, а не через обычный файловый сервер.

## ✨ Что вы получите

После запуска обоих сервисов вы увидите:

- **Дерзкий интерфейс** в темных и фиолетовых тонах 🔥
- **Фильтры** для поиска логов по проектам, уровням, IP, времени
- **Панель статистики** с uptime, количеством событий, ошибок
- **Таблицу аномальных IP** с визуальной индикацией угроз
- **Таблицу логов** с цветовой подсветкой и метаданными

## Следующие шаги

1. Интегрируйте API в ваше веб-расширение
2. Настройте CI/CD для автоматического деплоя
3. Добавьте авторизацию для продакшена
4. Настройте мониторинг и алерты

---

📚 Полная документация: 
- [Backend README](./backend/README.md)
- [Frontend README](./frontend/README.md)
- [Frontend Quick Start](./frontend/QUICKSTART.md)

