# ✅ Чек-лист деплоя Frontend

Быстрая памятка для деплоя фронтенда на Cloudflare Pages.

## 🚀 Перед деплоем

### 1. Проверка кода
- [ ] Все компоненты работают локально
- [ ] `npm run build` проходит без ошибок
- [ ] Нет ошибок в консоли браузера
- [ ] API запросы работают

### 2. Тестирование локально
```bash
npm run build
npm run preview
```
Проверьте http://localhost:4173

### 3. Подготовка репозитория
```bash
git add .
git commit -m "Готов к деплою"
git push origin main
```

---

## 📦 Cloudflare Pages - Первый деплой

### Настройки проекта

**Project name:**
```
logsfucker-frontend
```

**Production branch:**
```
main
```

**Build command:**
```bash
cd frontend && npm install && npm run build
```

**Build output directory:**
```
frontend/dist
```

**Environment variables:**
| Variable | Value |
|----------|-------|
| `NODE_VERSION` | `18` |

---

## 🔧 После деплоя

### Проверка

- [ ] Frontend открывается по URL
- [ ] Нет ошибок в консоли
- [ ] Фильтры работают
- [ ] API запросы идут
- [ ] Данные отображаются

### Тестовые запросы

1. Откройте DevTools → Network
2. Обновите страницу
3. Должны быть запросы к `/api/stats` и `/api/logs`
4. Status должен быть `200 OK`

---

## 🌐 Опционально

### Кастомный домен

1. Custom domains → Set up a custom domain
2. Введите домен: `logs.example.com`
3. Cloudflare настроит DNS автоматически
4. Подождите 1-2 минуты для SSL

### Preview деплои

Каждая новая ветка автоматически получит preview URL:
```
https://<branch-name>.logsfucker-frontend.pages.dev
```

---

## 🐛 Быстрые фиксы

### Build failed
```bash
# Проверьте локально
npm run build

# Если ошибки - исправьте и push снова
```

### 404 на странице
Убедитесь, что в `public/_redirects`:
```
/*  /index.html  200
```

### API не работает

**Если единый проект:**
- Проверьте `_routes.json` в корне
- Убедитесь, что Functions включены

**Если разные проекты:**
- Добавьте `VITE_API_URL` в environment variables
- Значение: `https://your-backend.pages.dev`

---

## 📋 Быстрые команды

### Локальная проверка
```bash
npm run build && npm run preview
```

### Просмотр логов сборки
Dashboard → Pages → Project → Deployments → View build log

### Redeploy
Dashboard → Pages → Project → Deployments → Retry deployment

### Rollback
Dashboard → Pages → Project → Deployments → выберите предыдущий → Rollback

---

## ✅ Готово!

После всех проверок ваш frontend в production! 🎉

**Детали:** [DEPLOYMENT.md](./DEPLOYMENT.md)

---

Made with 💜 and ⚡

