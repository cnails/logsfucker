# 🚀 Быстрый старт LogsFucker Frontend

## Шаг 1: Установка зависимостей

```bash
cd frontend
npm install
```

## Шаг 2: Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: **http://localhost:3000**

## Шаг 3: Убедитесь, что backend запущен

Frontend требует работающий backend на порту 8787. Запустите backend в отдельном терминале:

```bash
cd backend
npm install
npm run dev
```

## 🎨 Что вы увидите

### 1. **Фильтры** (верхняя панель)
   - Выбор проекта
   - Уровень логов (info/warn/error)
   - Поиск по IP
   - Временной период (24ч, 7д, 30д, custom)
   - Лимит записей

### 2. **Статистика** (средняя панель)
   - **Для одного проекта**: Uptime, Total событий, Errors/Success, Активные IP
   - **Для всех проектов**: Таблица со статистикой по каждому проекту

### 3. **Аномальные IP** (нижняя панель)
   - Таблица IP адресов с аномальной активностью
   - Визуальная индикация уровня угрозы
   - Подсветка критических аномалий

### 4. **Логи** (самая нижняя панель)
   - Таблица со всеми логами
   - Форматированное время
   - Цветовая индикация уровней
   - Метаданные в читаемом виде

## 🎨 Особенности дизайна

- **Темная тема** с фиолетовыми акцентами
- **Дерзкий стиль** с эмодзи и яркими цветами
- **Адаптивный дизайн** для всех устройств
- **Анимации** и hover-эффекты
- **Цветовая индикация** статусов и уровней

## 🛠 Разработка

### Структура файлов

```
src/
├── components/       # React компоненты
├── hooks/           # API хуки
├── pages/           # Страницы
├── types/           # TypeScript типы
├── utils/           # Утилиты
└── index.css        # Глобальные стили
```

### Добавление нового функционала

1. Создайте компонент в `src/components/`
2. Добавьте типы в `src/types/`
3. Создайте хук в `src/hooks/` если нужно
4. Используйте компонент в `src/pages/Dashboard.tsx`

## 📦 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🚀 Деплой на Cloudflare Pages

1. Подключите репозиторий к Cloudflare Pages
2. Настройте:
   - **Build command**: `cd frontend && npm install && npm run build`
   - **Build output directory**: `frontend/dist`
   - **Root directory**: `/`
3. Деплой!

## 🐛 Решение проблем

### Backend не отвечает
- Убедитесь, что backend запущен на порту 8787
- Проверьте прокси в `vite.config.ts`

### Ошибки TypeScript
```bash
npm run lint
```

### Проблемы с зависимостями
```bash
rm -rf node_modules package-lock.json
npm install
```

## 💡 Полезные команды

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Предпросмотр сборки
npm run preview

# Линтинг
npm run lint
```

## 🎉 Готово!

Теперь у вас работает дерзкий интерфейс для мониторинга логов! 🔥

Приятной разработки! 💜

