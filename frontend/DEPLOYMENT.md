# 🚀 Деплой Frontend на Cloudflare Pages

Подробное руководство по деплою фронтенда LogsFucker на Cloudflare Pages.

## 📋 Требования

- ✅ Аккаунт Cloudflare (бесплатный)
- ✅ Git репозиторий (GitHub, GitLab или Bitbucket)
- ✅ Backend уже задеплоен на Cloudflare Pages

## 🎯 Два способа деплоя

### Способ 1: Автоматический деплой через Git (рекомендуется)
### Способ 2: Ручной деплой через Wrangler CLI

---

## 🔥 Способ 1: Автоматический деплой (Git)

Это самый простой и рекомендуемый способ. При каждом push в репозиторий будет автоматически запускаться деплой.

### Шаг 1: Подготовка репозитория

Убедитесь, что ваш код в Git репозитории (GitHub, GitLab или Bitbucket).

```bash
# Если еще не инициализирован Git
git init
git add .
git commit -m "Initial commit: LogsFucker frontend"

# Пушим в remote репозиторий
git remote add origin <your-repo-url>
git push -u origin main
```

### Шаг 2: Создание проекта на Cloudflare Pages

1. Откройте [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. Перейдите в **Pages** в левом меню
3. Нажмите **Create a project**
4. Выберите **Connect to Git**

### Шаг 3: Подключение репозитория

1. Выберите вашего Git провайдера (GitHub/GitLab/Bitbucket)
2. Авторизуйте Cloudflare для доступа к репозиториям
3. Выберите репозиторий `logsfucker`
4. Нажмите **Begin setup**

### Шаг 4: Настройка сборки

На странице настройки проекта укажите:

#### Project name
```
logsfucker-frontend
```
(или любое другое имя)

#### Production branch
```
main
```
(или ваша основная ветка)

#### Framework preset
```
Vite
```
(Cloudflare автоматически определит настройки)

#### Build settings

**Build command:**
```bash
cd frontend && npm install && npm run build
```

**Build output directory:**
```
frontend/dist
```

**Root directory (optional):**
Оставьте пустым или укажите `/`

#### Environment variables (Build)

Добавьте переменные окружения, если нужно:

| Variable | Value | Description |
|----------|-------|-------------|
| `NODE_VERSION` | `18` | Версия Node.js |

### Шаг 5: Деплой!

1. Нажмите **Save and Deploy**
2. Cloudflare начнет сборку проекта
3. Процесс займет 2-3 минуты

Вы увидите лог сборки:
```
Installing dependencies...
Building project...
✅ Build succeeded!
Deploying to Cloudflare Pages...
✅ Deployment complete!
```

### Шаг 6: Получение URL

После успешного деплоя вы получите URL вида:
```
https://logsfucker-frontend.pages.dev
```

Откройте этот URL в браузере - вы должны увидеть дашборд LogsFucker! 🔥

---

## ⚙️ Способ 2: Ручной деплой через Wrangler

Для быстрого тестирования или если не хотите использовать Git.

### Шаг 1: Установка Wrangler

```bash
cd frontend
npm install -g wrangler
# или локально
npm install --save-dev wrangler
```

### Шаг 2: Авторизация

```bash
wrangler login
```

Откроется браузер для авторизации в Cloudflare.

### Шаг 3: Сборка проекта

```bash
npm run build
```

Это создаст папку `dist/` с готовыми файлами.

### Шаг 4: Деплой

```bash
wrangler pages deploy dist --project-name=logsfucker-frontend
```

При первом деплое Wrangler создаст новый проект на Cloudflare Pages.

**Вывод:**
```
✨ Success! Uploaded 15 files (2.34 sec)

✨ Deployment complete! Take a peek over at https://logsfucker-frontend.pages.dev
```

### Шаг 5: Повторный деплой

Для последующих деплоев:

```bash
npm run build
wrangler pages deploy dist --project-name=logsfucker-frontend
```

---

## 🔗 Настройка связи с Backend

После деплоя frontend и backend нужно настроить их взаимодействие.

### Вариант 1: Единый проект (рекомендуется)

Если backend и frontend в одном репозитории, можно настроить единый проект на Cloudflare Pages:

1. Backend (Functions) будет на `/api/*`
2. Frontend будет на всех остальных путях

**Настройка:**

В корне проекта создайте `_redirects` или `_routes.json`:

```json
{
  "version": 1,
  "include": ["/*"],
  "exclude": ["/api/*"]
}
```

Это позволит Frontend и Backend Functions работать вместе.

### Вариант 2: Разные проекты

Если frontend и backend в разных проектах Pages:

#### 1. Настройте CORS на backend

Backend уже настроен с CORS headers.

#### 2. Обновите API URL во frontend

Создайте `.env.production` в папке `frontend/`:

```env
VITE_API_URL=https://your-backend.pages.dev
```

Обновите `vite.config.ts`:

```typescript
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: process.env.VITE_API_URL || 'http://localhost:8787',
        changeOrigin: true,
      },
    },
  },
  // ... остальное
})
```

И в хуках используйте полный URL:

```typescript
// src/hooks/useStats.ts
const API_URL = import.meta.env.VITE_API_URL || '';
const response = await fetch(`${API_URL}/api/stats?${params.toString()}`);
```

---

## 🌐 Настройка кастомного домена

### Шаг 1: Добавление домена

1. В Cloudflare Pages откройте ваш проект
2. Перейдите в **Custom domains**
3. Нажмите **Set up a custom domain**
4. Введите ваш домен: `logs.example.com`
5. Cloudflare автоматически настроит DNS

### Шаг 2: Настройка DNS

Если домен НЕ на Cloudflare:

Добавьте CNAME запись:
```
logs.example.com  CNAME  logsfucker-frontend.pages.dev
```

Если домен на Cloudflare:
DNS настроится автоматически! ✨

### Шаг 3: SSL/TLS

Cloudflare автоматически настроит SSL сертификат.
Подождите 1-2 минуты, и ваш сайт будет доступен по HTTPS.

---

## 🔄 Автоматические деплои

При использовании Git интеграции:

### Production деплои
- Каждый push в `main` ветку → автоматический деплой в production
- URL: `https://logsfucker-frontend.pages.dev`

### Preview деплои
- Каждый push в другие ветки → preview деплой
- URL: `https://<branch-name>.logsfucker-frontend.pages.dev`

### Pull Request previews
- Каждый PR → отдельный preview деплой
- URL будет в комментарии к PR

---

## 🎨 Оптимизация для продакшена

### 1. Build оптимизации

В `vite.config.ts` уже настроено:

```typescript
build: {
  outDir: 'dist',
  sourcemap: true,
  minify: 'terser',
  rollupOptions: {
    output: {
      manualChunks: {
        vendor: ['react', 'react-dom'],
        utils: ['date-fns'],
      },
    },
  },
}
```

### 2. Caching headers

Cloudflare Pages автоматически настроит оптимальные cache headers для статических ресурсов.

### 3. CDN

Ваш frontend будет автоматически доступен через глобальную CDN Cloudflare из 200+ локаций.

---

## 📊 Мониторинг деплоев

### Просмотр логов деплоя

1. Откройте Cloudflare Dashboard → Pages
2. Выберите ваш проект
3. Вкладка **Deployments**
4. Кликните на деплой → **View build logs**

### Статистика

В проекте доступна аналитика:
- Запросы
- Bandwidth
- Уникальные посетители
- География запросов

---

## 🐛 Решение проблем

### "Build failed: Command not found"

**Причина:** Node.js не установлен или неправильная версия.

**Решение:**
1. Добавьте environment variable: `NODE_VERSION=18`
2. Пересоберите проект

### "404 Not Found" после деплоя

**Причина:** Неправильный путь к build output.

**Решение:**
Убедитесь, что в настройках указано:
```
Build output directory: frontend/dist
```

### "API не отвечает"

**Причина:** Frontend не может подключиться к backend.

**Решение:**

Для единого проекта:
1. Убедитесь, что backend Functions в папке `functions/`
2. Проверьте `_routes.json`

Для разных проектов:
1. Настройте CORS на backend
2. Укажите `VITE_API_URL` в environment variables
3. Обновите хуки для использования полного URL

### "Module not found" ошибки

**Причина:** Зависимости не установлены.

**Решение:**
Проверьте build command:
```bash
cd frontend && npm install && npm run build
```

### Медленная сборка

**Причина:** Большой node_modules.

**Решение:**
1. Используйте npm ci вместо npm install:
   ```bash
   cd frontend && npm ci && npm run build
   ```
2. Добавьте кэширование (Cloudflare Pages делает это автоматически)

---

## 🔒 Безопасность

### Environment variables для секретов

Если нужны API ключи или секреты:

1. В Cloudflare Pages → Settings → Environment variables
2. Добавьте переменные (они будут зашифрованы)
3. Используйте в коде: `import.meta.env.VITE_API_KEY`

**Важно:** Имена должны начинаться с `VITE_` чтобы быть доступными в браузере.

### Headers безопасности

Создайте `_headers` в `public/`:

```
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=()
```

---

## 📈 Масштабирование

Cloudflare Pages автоматически масштабируется:
- ✅ Безлимитные запросы
- ✅ Безлимитный bandwidth (на Free плане тоже!)
- ✅ Глобальная CDN
- ✅ DDoS защита
- ✅ 99.99% uptime

---

## ✅ Чек-лист деплоя

- [ ] Backend задеплоен и работает
- [ ] Код в Git репозитории
- [ ] Проект создан на Cloudflare Pages
- [ ] Build settings настроены правильно
- [ ] Environment variables добавлены (если нужно)
- [ ] Первый деплой успешен
- [ ] Frontend открывается по URL
- [ ] API запросы работают
- [ ] Кастомный домен настроен (опционально)
- [ ] SSL сертификат активен

---

## 🎉 Готово!

Ваш frontend теперь задеплоен на Cloudflare Pages и доступен по всему миру! 🌍

**Полезные ссылки:**
- [Cloudflare Pages Docs](https://developers.cloudflare.com/pages/)
- [Vite Build Optimization](https://vitejs.dev/guide/build.html)
- [Frontend README](./README.md)

---

## 🚀 Следующие шаги

1. **Настройте мониторинг** - добавьте аналитику (Google Analytics, Plausible)
2. **Добавьте алерты** - настройте уведомления о падении сервиса
3. **Оптимизируйте** - используйте Lighthouse для проверки производительности
4. **Тестируйте** - добавьте E2E тесты с Playwright

---

Made with 💜 and ⚡ by LogsFucker Team

