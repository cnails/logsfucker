# 🎨 Возможности фронтенда LogsFucker

## 🔥 Дизайн

### Дерзкий стиль
- **Темная тема** - основа на темно-сером (#18181b)
- **Фиолетовые акценты** - primary цвет (#8b5cf6)
- **Эмодзи** - живой и современный интерфейс
- **Анимации** - hover эффекты, плавные переходы, pulse
- **Градиенты** - для заголовков и акцентов

### Адаптивность
- Полностью адаптивная верстка
- Mobile-first подход
- Responsive grid для всех компонентов
- Оптимизация для экранов от 320px до 4K

## 📊 Компоненты

### 1. Filters (Фильтры)

**Функционал:**
- Выбор проекта (extensionName) из dropdown
- Фильтр по уровню (all/info/warn/error)
- Поиск по IP адресу
- Быстрые временные периоды (24ч, 7д, 30д)
- Кастомный период (from/to)
- Настройка лимита записей (10-1000)
- Кнопка "Обновить данные"

**Особенности:**
- Автоматическая загрузка при старте (последние 24 часа)
- Валидация временных периодов
- Сохранение состояния фильтров

### 2. StatsPanel (Панель статистики)

**Для одного проекта:**
- **Uptime** - процент успешных запросов
  - Зеленый ≥95%
  - Желтый ≥80%
  - Красный <80%
- **Total** - общее количество событий
- **Errors/Success** - соотношение ошибок к успешным
- **Active IPs** - количество уникальных IP

**Для всех проектов:**
- Таблица со статистикой по каждому проекту
- Колонки: Проект, Uptime, Всего, Ошибок, IP адресов, Аномалий
- Цветовая индикация uptime
- Подсветка аномалий

**Особенности:**
- Loading skeleton
- Error handling
- Empty state
- Hover эффекты

### 3. AnomaliesTable (Аномальные IP)

**Функционал:**
- Отображение IP с аномальной активностью
- Сортировка по количеству запросов (убывание)
- Фильтрация по проектам
- Визуальная индикация уровня угрозы

**Уровни аномалий:**
- 🔴 **Критическая** - isAnomaly = true && count > 500
- ⚠️ **Аномалия** - isAnomaly = true
- ⚡ **Высокая активность** - count > 100

**Особенности:**
- Цветная подсветка строк
- Tooltip с подсказками
- Empty state когда аномалий нет
- Поддержка неизвестных IP

### 4. LogsTable (Таблица логов)

**Функционал:**
- Отображение всех логов с фильтрацией
- Колонки: Время, Проект, IP, Уровень, Сообщение, Мета
- Форматирование времени (dd MMM yyyy HH:mm:ss)
- Парсинг метаданных из JSON

**Уровни логов:**
- 🔴 **Error** - красный бейдж
- ⚠️ **Warn** - желтый бейдж
- ℹ️ **Info** - синий бейдж

**Особенности:**
- Truncate для длинных сообщений
- Tooltip с полным текстом
- Статистика внизу (info/warn/error count)
- Loading skeleton
- Empty state

## 🎯 API хуки

### useStats(filters)

```typescript
const { data, loading, error, refetch } = useStats({
  extensionName: 'my-extension',
  from: Date.now() - 24 * 60 * 60 * 1000,
  to: Date.now(),
});
```

**Возвращает:**
- `data` - массив ProjectStats[]
- `loading` - boolean
- `error` - string | null
- `refetch` - функция для перезагрузки

### useLogs(filters)

```typescript
const { data, loading, error, refetch } = useLogs({
  extensionName: 'my-extension',
  level: 'error',
  ip: '1.2.3.4',
  from: Date.now() - 24 * 60 * 60 * 1000,
  to: Date.now(),
  limit: 100,
});
```

**Возвращает:**
- `data` - массив LogEntry[]
- `loading` - boolean
- `error` - string | null
- `refetch` - функция для перезагрузки

## 🎨 Дизайн-система

### Цвета

```css
/* Primary (Фиолетовый) */
primary-400: #a78bfa
primary-500: #8b5cf6
primary-600: #7c3aed
primary-700: #6d28d9

/* Dark (Серый) */
dark-50: #18181b
dark-100: #27272a
dark-200: #3f3f46
dark-500: #a1a1aa
dark-600: #d4d4d8

/* Статусы */
info: blue-400
warn: yellow-400
error: red-400
success: green-400
```

### Компоненты

**Контейнер:**
```jsx
<div className="bg-dark-100 rounded-lg p-6 shadow-xl border border-primary-900/30">
```

**Заголовок:**
```jsx
<h2 className="text-2xl font-bold text-primary-400 mb-4 flex items-center gap-2">
  <span className="text-3xl">🔥</span>
  Заголовок
</h2>
```

**Кнопка:**
```jsx
<button className="bg-primary-600 hover:bg-primary-700 text-white font-bold py-3 px-8 rounded-lg transition-colors duration-200 shadow-lg hover:shadow-primary-500/50">
  Действие
</button>
```

**Input:**
```jsx
<input className="w-full bg-dark-50 border border-dark-200 text-dark-800 rounded-lg px-4 py-2 focus:outline-none focus:ring-2 focus:ring-primary-500" />
```

## ⚡ Оптимизации

### Производительность

- **useMemo** для тяжелых вычислений
- **useCallback** для функций в зависимостях
- **React.memo** для дорогих компонентов (при необходимости)
- Оптимизация re-renders

### UX

- Loading состояния (skeleton)
- Error handling с понятными сообщениями
- Empty states с подсказками
- Debounce для поисковых input'ов (можно добавить)
- Оптимистичные обновления

## 🚀 Будущие улучшения

### Возможные фичи

- [ ] **Графики** - Charts для визуализации трендов
- [ ] **Экспорт** - CSV/JSON экспорт логов
- [ ] **Уведомления** - Real-time алерты при критических событиях
- [ ] **Фильтры** - Сохранение наборов фильтров
- [ ] **Dark/Light** - Переключение тем
- [ ] **Пагинация** - Для больших наборов данных
- [ ] **Поиск** - Полнотекстовый поиск по сообщениям
- [ ] **Детали** - Модальное окно с деталями лога
- [ ] **Автообновление** - Периодическая перезагрузка данных

### Технические улучшения

- [ ] Добавить тесты (Jest + React Testing Library)
- [ ] Добавить Storybook для компонентов
- [ ] Добавить E2E тесты (Playwright)
- [ ] Добавить React Query для кеширования
- [ ] Добавить Zustand для state management
- [ ] Оптимизировать bundle size
- [ ] Добавить PWA support

## 📱 Адаптивность

### Breakpoints

- **Mobile**: < 768px - стек колонок
- **Tablet**: 768px - 1024px - 2 колонки
- **Desktop**: > 1024px - 4 колонки
- **Wide**: > 1536px - полная ширина

### Mobile оптимизации

- Сенсорные таргеты ≥44px
- Упрощенная навигация
- Горизонтальная прокрутка таблиц
- Collapsed фильтры

## 🎉 Итог

Фронтенд LogsFucker - это:
- 🔥 **Дерзкий** дизайн
- ⚡ **Быстрый** и отзывчивый
- 🎨 **Красивый** интерфейс
- 📊 **Информативный** дашборд
- 🛡️ **Типобезопасный** TypeScript

---

Made with 💜 and ⚡ by LogsFucker Team

