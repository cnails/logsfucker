# LogsFucker Frontend 🚀

Дерзкий и современный интерфейс для мониторинга логов веб-расширений.

## 🎨 Дизайн

- **Стиль**: Дерзкий, броский, современный
- **Цветовая схема**: Темные и фиолетовые тона
- **UI Framework**: TailwindCSS
- **Компоненты**: React + TypeScript

## 🛠 Технологии

- **React 18** с TypeScript
- **Vite** для быстрой разработки
- **TailwindCSS** для стилизации
- **date-fns** для работы с датами
- **Строгий TypeScript** для надежности кода

## 📦 Установка

```bash
npm install
```

## 🚀 Запуск

### Режим разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для продакшена

```bash
npm run build
```

### Предпросмотр продакшен сборки

```bash
npm run preview
```

## 🏗 Структура проекта

```
frontend/
├── src/
│   ├── components/          # React компоненты
│   │   ├── Filters.tsx      # Фильтры для поиска логов
│   │   ├── StatsPanel.tsx   # Панель статистики
│   │   ├── AnomaliesTable.tsx  # Таблица аномальных IP
│   │   └── LogsTable.tsx    # Таблица логов
│   ├── hooks/               # Кастомные хуки
│   │   ├── useStats.ts      # Хук для загрузки статистики
│   │   └── useLogs.ts       # Хук для загрузки логов
│   ├── pages/               # Страницы
│   │   └── Dashboard.tsx    # Главная страница дашборда
│   ├── types/               # TypeScript типы
│   │   └── api.ts           # Типы для API
│   ├── utils/               # Утилиты
│   │   └── date.ts          # Работа с датами
│   ├── App.tsx              # Главный компонент
│   ├── main.tsx             # Точка входа
│   └── index.css            # Глобальные стили
├── index.html
├── package.json
├── vite.config.ts
├── tsconfig.json
└── tailwind.config.js
```

## 📊 Основные возможности

### 1. Фильтрация логов

- Выбор проекта (extension)
- Фильтр по уровню (info/warn/error)
- Поиск по IP
- Временные диапазоны (24ч, 7д, 30д, custom)
- Лимит записей

### 2. Панель статистики

**Для одного проекта:**
- Uptime (процент успешных запросов)
- Общее количество событий
- Соотношение ошибок к успешным запросам
- Количество активных IP

**Для всех проектов:**
- Таблица со статистикой по каждому проекту
- Uptime, количество событий, ошибки, IP адреса

### 3. Аномальные IP

- Автоматическое выявление аномально активных IP
- Визуальная индикация уровня угрозы
- Подсветка критических аномалий

### 4. Таблица логов

- Отображение всех логов с фильтрацией
- Форматирование времени
- Цветовая индикация уровней
- Парсинг метаданных
- Статистика по уровням внизу

## 🎨 Дизайн-система

### Цвета

- **Primary (Фиолетовый)**: `#8b5cf6` - основной акцентный цвет
- **Dark**: От `#18181b` до `#fafafa` - градации серого
- **Info**: Синий - для информационных сообщений
- **Warning**: Желтый - для предупреждений
- **Error**: Красный - для ошибок

### Компоненты

Все компоненты следуют единому стилю:
- Закругленные углы (rounded-lg)
- Тени и бордеры для глубины
- Hover-эффекты для интерактивности
- Адаптивная сетка (responsive grid)

## 🔌 API Integration

Frontend работает с двумя основными эндпоинтами:

### GET /api/logs

Параметры:
- `extensionName` - название проекта
- `level` - уровень лога
- `ip` - IP адрес
- `from` - начало периода (timestamp ms)
- `to` - конец периода (timestamp ms)
- `limit` - максимум записей

### GET /api/stats

Параметры:
- `extensionName` - название проекта (опционально)
- `from` - начало периода (timestamp ms)
- `to` - конец периода (timestamp ms)

## 🚀 Деплой на Cloudflare Pages

Frontend готов к деплою на Cloudflare Pages:

### Быстрый способ

1. Подключите репозиторий к Cloudflare Pages
2. Настройте build:
   - Build command: `cd frontend && npm install && npm run build`
   - Build output directory: `frontend/dist`
3. Деплой!

### Подробная инструкция

См. **[DEPLOYMENT.md](./DEPLOYMENT.md)** для детального руководства, включая:
- ✅ Автоматический деплой через Git
- ✅ Ручной деплой через Wrangler CLI
- ✅ Настройка кастомного домена
- ✅ Связь frontend с backend
- ✅ Оптимизации для продакшена
- ✅ Решение проблем при деплое

## 📝 Разработка

### Добавление нового компонента

```typescript
// src/components/MyComponent.tsx
interface MyComponentProps {
  // ...
}

export function MyComponent({ }: MyComponentProps) {
  return (
    <div className="bg-dark-100 rounded-lg p-6">
      {/* Ваш компонент */}
    </div>
  );
}
```

### Создание нового хука

```typescript
// src/hooks/useMyHook.ts
import { useState, useEffect } from 'react';

export function useMyHook() {
  // Ваша логика
}
```

## 🤝 Contributing

См. [CONTRIBUTING.md](../CONTRIBUTING.md) в корне проекта.

## 📄 Лицензия

MIT License - см. [LICENSE](../LICENSE)

## 🎉 Статус

✅ **Готово к использованию!**

---

Made with 💜 and ⚡ by LogsFucker Team
