# 🤝 Вклад в проект

Спасибо за интерес к проекту LogsFucker! Мы приветствуем любой вклад.

## Структура проекта

```
logsfucker/
├── backend/           # Backend на Cloudflare Pages + D1
├── frontend/          # Frontend (в разработке)
└── docs/             # Документация
```

## Как внести изменения

### 1. Форк и клонирование

```bash
git clone https://github.com/your-username/logsfucker.git
cd logsfucker
```

### 2. Настройка backend

```bash
cd backend
npm install
wrangler d1 create logs-db
# Обновите database_id в wrangler.toml
npm run d1:migrate
npm run dev
```

### 3. Внесение изменений

Создайте новую ветку:

```bash
git checkout -b feature/my-new-feature
```

### 4. Тестирование

```bash
# Backend
cd backend
npm run dev

# Генерация тестовых данных
npm run test:generate
```

### 5. Коммит

```bash
git add .
git commit -m "feat: добавлена новая функция"
```

Используйте [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.д.

### 6. Push и Pull Request

```bash
git push origin feature/my-new-feature
```

Создайте Pull Request на GitHub с описанием ваших изменений.

## Разработка

### Backend

```bash
cd backend
npm run dev           # Локальная разработка
npm run deploy        # Деплой на Cloudflare
npm run d1:migrate    # Применить миграции
npm run test:generate # Генерация тестовых данных
```

### Frontend (когда будет готово)

```bash
cd frontend
npm install
npm run dev
npm run build
```

## Рекомендации по коду

### TypeScript

- Используйте строгую типизацию
- Избегайте `any`, используйте `unknown` если тип неизвестен
- Документируйте сложную логику комментариями

### API

- Всегда возвращайте JSON с понятными сообщениями об ошибках
- Используйте правильные HTTP статусы
- Добавляйте CORS заголовки для всех endpoints

### База данных

- Создавайте новые миграции для изменения схемы
- Не изменяйте существующие миграции
- Добавляйте индексы для часто используемых запросов

## Создание миграций

```bash
cd backend

# Создайте новый файл миграции
# migrations/XXXX_description.sql

# Применить локально
wrangler d1 migrations apply logs-db --local

# Применить на production
wrangler d1 migrations apply logs-db
```

## Отчёты об ошибках

При создании issue, пожалуйста, укажите:

- Шаги для воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Версию Node.js и Wrangler
- Логи ошибок

## Предложения функций

Если у вас есть идея новой функции:

1. Проверьте, нет ли уже подобного issue
2. Создайте новый issue с меткой `enhancement`
3. Опишите функцию и её применение
4. Дождитесь обсуждения перед началом работы

## Стиль кода

Мы используем TypeScript с strict mode. Перед коммитом убедитесь, что:

- Код компилируется без ошибок
- Нет ошибок линтера
- Код читаем и хорошо структурирован

## Вопросы?

Если у вас есть вопросы, создайте issue с меткой `question`.

Спасибо за вклад! 🙌

