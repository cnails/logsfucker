name = "logsfucker"
compatibility_date = "2024-01-01"

# Куда кладёт сборка фронта
pages_build_output_dir = "frontend/dist"

# D1 bindings настраиваются через Dashboard (Settings → Functions → D1 database bindings)
# Не раскомментируйте [[d1_databases]] - это конфликтует с Dashboard

[env.production]
vars = { ENVIRONMENT = "production" }

[env.preview]
vars = { ENVIRONMENT = "preview" }
