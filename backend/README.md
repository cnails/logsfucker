# LogsFucker Backend - Микросервис логов

Backend для сбора, хранения и анализа логов от веб-расширений на базе Cloudflare Pages + D1.

## Возможности

- 📥 Приём событий от веб-расширений
- 💾 Хранение в D1 (SQLite на Cloudflare)
- 🔍 Получение сырых логов с фильтрацией
- 📊 Агрегированная статистика по проектам
- 🚨 Выявление аномально активных IP
- ✅ Расчёт uptime (процент успешных событий)
- 🌐 Без авторизации, пользователь определяется по IP

## Технологии

- TypeScript
- Cloudflare Pages Functions
- Cloudflare D1 (SQLite)
- Wrangler CLI

## Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Создание D1 базы данных

Создайте новую D1 базу данных:

```bash
wrangler d1 create logs-db
```

Команда выведет информацию о созданной БД. Скопируйте `database_id` и обновите его в `wrangler.toml`:

```toml
[[d1_databases]]
binding = "DB"
database_name = "logs-db"
database_id = "ваш-database-id"  # Вставьте реальный ID
```

### 3. Применение миграций

Примените SQL миграции для создания таблиц и индексов:

```bash
npm run d1:migrate
```

Или напрямую через wrangler:

```bash
wrangler d1 migrations apply logs-db
```

Для просмотра списка миграций:

```bash
npm run d1:list
```

### 4. Локальная разработка

Запустите локальный dev-сервер:

```bash
npm run dev
```

Сервер будет доступен по адресу `http://localhost:8788`

### 5. Деплой на Cloudflare Pages

Первый деплой (создание проекта):

```bash
npm run deploy
```

Wrangler предложит создать новый Pages проект. После создания все последующие деплои будут обновлять этот проект.

## API Endpoints

### POST /api/logs

Приём логов от расширений.

**Запрос:**

```json
{
  "extensionName": "my-extension",
  "level": "info",           // опционально, по умолчанию "info"
  "message": "Something happened",
  "meta": {                  // опционально, любой объект
    "userId": 123,
    "action": "click"
  },
  "ts": 1699999999999        // опционально, timestamp в ms
}
```

**Ответ (успех):**

```json
{
  "ok": true
}
```

**Ошибки:**

- 400 - отсутствуют обязательные поля
- 500 - ошибка БД или некорректный JSON

---

### GET /api/logs

Получение логов с фильтрацией.

**Query-параметры:**

- `extensionName` - фильтр по имени расширения
- `level` - фильтр по уровню (info, error, warning и т.д.)
- `ip` - фильтр по IP адресу
- `from` - начало периода (timestamp в ms)
- `to` - конец периода (timestamp в ms)
- `limit` - максимум записей (по умолчанию 100, макс. 1000)

**Пример запроса:**

```
GET /api/logs?extensionName=my-extension&level=error&limit=50
```

**Ответ:**

```json
[
  {
    "id": 123,
    "extension_name": "my-extension",
    "level": "error",
    "message": "Failed to fetch data",
    "meta": "{\"code\":404}",
    "ip": "1.2.3.4",
    "created_at": 1699999999999
  }
]
```

---

### GET /api/stats

Получение агрегированной статистики по проектам.

**Query-параметры:**

- `extensionName` - статистика только по одному проекту (опционально)
- `from` - начало периода (timestamp в ms, опционально)
- `to` - конец периода (timestamp в ms, опционально)

Если `from`/`to` не указаны, используется период последних 24 часов.

**Пример запроса:**

```
GET /api/stats?extensionName=my-extension
```

**Ответ:**

```json
[
  {
    "extensionName": "my-extension",
    "total": 1000,
    "errorCount": 50,
    "successCount": 950,
    "uptimePercent": 95.0,
    "ips": {
      "totalIps": 25,
      "topIps": [
        {
          "ip": "1.2.3.4",
          "count": 500,
          "isAnomaly": true
        },
        {
          "ip": "5.6.7.8",
          "count": 100,
          "isAnomaly": false
        }
      ]
    }
  }
]
```

**Определение аномальных IP:**

IP считается аномальным, если его активность >= среднее * 3.

---

### OPTIONS /api/logs и /api/stats

CORS preflight запросы. Возвращают статус 204 с необходимыми CORS заголовками.

## Структура проекта

```
backend/
├── functions/
│   └── api/
│       ├── logs.ts      # API для логов (POST, GET)
│       └── stats.ts     # API для статистики (GET)
├── migrations/
│   └── 0001_init.sql    # Миграция создания таблицы logs
├── scripts/
│   └── generate-test-data.js  # Генератор тестовых данных
├── example-client.html  # Тестовый UI
├── package.json
├── tsconfig.json
└── wrangler.toml        # Конфигурация Cloudflare
```

## Схема базы данных

### Таблица `logs`

| Поле           | Тип     | Описание                          |
|----------------|---------|-----------------------------------|
| id             | INTEGER | Primary key, autoincrement        |
| extension_name | TEXT    | Название расширения               |
| level          | TEXT    | Уровень лога (info, error и т.д.) |
| message        | TEXT    | Сообщение лога                    |
| meta           | TEXT    | JSON с доп. данными (nullable)    |
| ip             | TEXT    | IP пользователя (nullable)        |
| created_at     | INTEGER | Timestamp в миллисекундах         |

**Индексы:**

- `idx_logs_extension_time` - по (extension_name, created_at DESC)
- `idx_logs_extension_ip_time` - по (extension_name, ip, created_at DESC)
- `idx_logs_level_time` - по (level, created_at DESC)

## Примеры использования

### Отправка лога из расширения

```javascript
// В вашем веб-расширении
async function sendLog(level, message, meta = {}) {
  try {
    await fetch('https://your-pages-url.pages.dev/api/logs', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        extensionName: 'my-awesome-extension',
        level,
        message,
        meta,
        ts: Date.now(),
      }),
    });
  } catch (error) {
    console.error('Failed to send log:', error);
  }
}

// Использование
sendLog('info', 'User clicked button', { buttonId: 'submit' });
sendLog('error', 'API request failed', { endpoint: '/api/users', status: 500 });
```

### Получение логов

```javascript
// Получить последние 50 логов с ошибками
const response = await fetch(
  'https://your-pages-url.pages.dev/api/logs?level=error&limit=50'
);
const logs = await response.json();
console.log(logs);
```

### Получение статистики

```javascript
// Статистика за последние 24 часа по всем проектам
const response = await fetch('https://your-pages-url.pages.dev/api/stats');
const stats = await response.json();

stats.forEach(project => {
  console.log(`${project.extensionName}: ${project.uptimePercent}% uptime`);
  
  // Найти аномальные IP
  const anomalies = project.ips.topIps.filter(ip => ip.isAnomaly);
  console.log('Anomalous IPs:', anomalies);
});
```

## Полезные команды

```bash
# Локальная разработка
npm run dev

# Деплой на Cloudflare Pages
npm run deploy

# Применить миграции
npm run d1:migrate

# Список миграций
npm run d1:list

# Генерация тестовых данных
npm run test:generate

# Прямой SQL запрос к D1 (для отладки)
wrangler d1 execute logs-db --command "SELECT * FROM logs LIMIT 10"
```

## Особенности работы

1. **Определение IP**: Используются заголовки CF-Connecting-IP, x-forwarded-for или remote-addr
2. **Без авторизации**: Любой может отправлять логи, пользователь определяется по IP
3. **CORS**: Разрешены запросы с любых доменов (Access-Control-Allow-Origin: *)
4. **Лимиты**: Максимум 1000 записей за один запрос GET /api/logs
5. **Uptime**: Считается как (total - errorCount) / total * 100%
6. **Аномальные IP**: Активность >= среднее * 3

## Безопасность

⚠️ **Внимание**: Этот сервис не имеет авторизации и открыт для публичного использования.

Для продакшена рекомендуется:

- Добавить API ключи или токены авторизации
- Ограничить rate limit на уровне Cloudflare
- Настроить WAF правила для защиты от злоупотреблений
- Мониторить использование и затраты D1

## Дополнительная документация

- [DEPLOYMENT.md](./DEPLOYMENT.md) - детальная инструкция по развертыванию
- [INTEGRATION_EXAMPLES.md](./INTEGRATION_EXAMPLES.md) - примеры интеграции для различных платформ

## Лицензия

MIT

